[workspace]
members = [
	#"tiled",
	#"tiler",

	"ex",
	"renderer",

	"asprite",

	"ui",
	"math",
]

[features]
profiler = ["thread_profiler/thread_profiler"]

[dependencies]
thread_profiler = { version = "0.1", optional = true }

[profile.release]
lto = true
panic = 'abort'
